using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class EnemyController : UnitController {

    public BaseEnemy pawn;

    static PlayerController Singleton;

    private void Awake()
    {
        damageHandler = GetComponent<DamageHandler>();
        inventoryHandler = GetComponent<InventoryHandler>();
    }

    private void Update()
    {
        inventoryHandler.attackTimer -= Time.deltaTime;
        damageHandler.RegenerateHealth();
    }

    private void FixedUpdate()
    {
        pawn.currentTarget = PlayerController.GetInstance().pawn;

        pawn.OnSelectEnemy(pawn.currentTarget);
        //pawn.currentDestination = pawn.currentTarget.transform.position;
        //pawn.OnSelectDestination(pawn.currentTarget.transform.position);
    }
}*/
