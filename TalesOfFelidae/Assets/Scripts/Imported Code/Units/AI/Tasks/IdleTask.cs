using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
[CreateAssetMenu(menuName = "Tasks/IdleTask")]
public class IdleTask : BaseTask {

    public override void ExecuteFixedUpdate(UnitPawn unit)
    {
        unit.animController.SetBool(unit.walkingHash, false);
        unit.animController.SetBool(unit.attacking1HHash, false);
        unit.animController.SetBool(unit.attacking2HHash, false);
        //unit.animController.SetBool(unit.usingPowerHash, false);
        return;
    }

    public override bool TestKeys(UnitPawn unit)
    {
        return true;
    }
}*/
